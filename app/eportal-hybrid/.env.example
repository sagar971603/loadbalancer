# Primary in-process browser-like HTTP transport.
EP_PORTAL_HTTP_TRANSPORT=curl_cffi

# Change only if curl_cffi needs another browser fingerprint.
EP_PORTAL_CURL_CFFI_IMPERSONATE=chrome120

# Optional fallback if using EP_PORTAL_HTTP_TRANSPORT=curl or auto.
# EP_PORTAL_CURL=/usr/bin/curl
