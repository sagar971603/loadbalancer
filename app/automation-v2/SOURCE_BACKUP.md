# Automation V2 source-only backup

This directory contains the Python application and dependency manifests copied from the deployed backend on 14 July 2026.

It excludes `.env` files, credentials, logs, PDFs, downloads, caches, browser binaries, virtual environments, and other runtime data. Example identities and passwords were sanitized before this public backup was committed, and credential-printing code was removed from this copy.
